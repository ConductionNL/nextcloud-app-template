name: Branch Protection

on:
  pull_request:
    branches: [beta] # main: disabled until ruleset check-branch context is fixed (should be 'protect / check-branch')

jobs:
  protect:
    uses: ConductionNL/.github/.github/workflows/branch-protection.yml@main
    secrets: inherit
