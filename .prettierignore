js/
dist/
build/
node_modules/
vendor/
coverage/
docs/
*.min.*

# Generated by the translation workflow — `l10n/*.js` is emitted by
# scripts/build-l10n-js.js (`OC.L10N.register(...)`, four-space indent).
# NOT gitignored, so this entry is
# load-bearing: without it prettier and `npm run l10n:build` rewrite the same
# 38 files in opposite directions and the format check fails forever. Matches
# the entry dossiq already carries.
l10n/
